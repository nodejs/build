# https://beta.ruff.rs
name: Lint Python
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  lint_python:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: pip install --user ruff
    - run: ruff --format=github .
